<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.checkingscreens">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sound_bytes"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sound_bytes_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ActivityAllArtists"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            tools:ignore="LockedOrientationActivity" />

        <activity android:name=".ActivityAllAlbums"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            tools:ignore="LockedOrientationActivity"/>

        <activity android:name=".ActivityArtist"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            tools:ignore="LockedOrientationActivity"/>

        <activity android:name=".ActivityAlbum"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            tools:ignore="LockedOrientationActivity"/>

        <activity android:name=".MainActivity"
            android:parentActivityName=".SplashScreen"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            tools:ignore="LockedOrientationActivity"/>

        <activity android:name=".SplashScreen"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            tools:ignore="LockedOrientationActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>